# meteor-reload-of-death

- Two Meteor apps here
- `app` connects to `remote-app` using DDP_DEFAULT_CONNECTION_URL
- Run both apps (you can use the convenience `meteor` script to run the apps)
- Watch `app` constantly reload
